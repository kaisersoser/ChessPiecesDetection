<Page
    x:Class="ChessPiecesDetection.PiecesDBDataView"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ChessPiecesDetection"    
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel Orientation="Vertical">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="150" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="1" Name="PageTitle" Text="Review Database"/>
                <TextBlock Grid.Column="2" Name="RowsInDB" />
            </Grid>

            <controls:DataGrid x:Name="PiecesTableDataGrid"
                               Height="800"
                               Margin="10,10"
                               AutoGenerateColumns="False"/>
        </StackPanel>
        
        
    </Grid>
</Page>
